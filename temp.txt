"nitrostack": {
    "command": "node",
    "args": [
        "dist/index.js"
    ],
    "env": {
        "NODE_ENV": "development"
    }
},
"slack": {
    "command": "npx",
    "args": [
        "-y",
        "@modelcontextprotocol/server-slack"
    ],
    "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
    }
},
"gmail": {
    "command": "npx",
    "args": [
        "-y",
        "@martinzarfl/mail-mcp"
    ],
    "env": {
        "SMTP_HOST": "${SMTP_HOST}",
        "SMTP_PORT": "${SMTP_PORT}",
        "SMTP_USER": "${SMTP_USER}",
        "SMTP_PASS": "${SMTP_PASS}",
        "SMTP_FROM": "balaji.natarajanece@gmail.com"
    }
},